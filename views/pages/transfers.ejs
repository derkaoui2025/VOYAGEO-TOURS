<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    
    <!-- Primary Meta Tags -->
    <title>Morocco City Transfers | Private Transport Services | Voyageo Tours</title>
    <meta name="title" content="Morocco City Transfers | Private Transport Services | Voyageo Tours">
    <meta name="description" content="Reliable and comfortable transfers between Moroccan cities. Private cars, minivans, and luxury vehicles with professional drivers.">
    <meta name="keywords" content="Morocco transfers, airport transfer Morocco, Marrakech to Fes transfer, Casablanca airport transfer, private transfers Morocco, city transfers Morocco">
    <meta name="author" content="Voyageo Tours">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://voyageo-tours.com/transfers">
    <meta property="og:title" content="Morocco City Transfers | Private Transport Services | Voyageo Tours">
    <meta property="og:description" content="Reliable and comfortable transfers between Moroccan cities. Private cars, minivans, and luxury vehicles with professional drivers.">
    <meta property="og:image" content="/images/headers/morocco-transfer.jpg">
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="https://voyageo-tours.com/transfers">
    <meta property="twitter:title" content="Morocco City Transfers | Private Transport Services | Voyageo Tours">
    <meta property="twitter:description" content="Reliable and comfortable transfers between Moroccan cities. Private cars, minivans, and luxury vehicles with professional drivers.">
    <meta property="twitter:image" content="/images/headers/morocco-transfer.jpg">
    
    <!-- Canonical Link -->
    <link rel="canonical" href="https://voyageo-tours.com/transfers">
    
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/images/favicon.png">
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=Urbanist:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#B8860B',
                        secondary: '#252525',
                        tertiary: '#86644B',
                        beige: '#F5F1E9',
                        cream: '#F9F6F0',
                        stone: '#A49E8D',
                        charcoal: '#1F1F1F'
                    },
                    fontFamily: {
                        serif: ['Cormorant Garamond', 'serif'],
                        sans: ['Urbanist', 'sans-serif']
                    },
                    fontSize: {
                        '4.5xl': '2.5rem',
                        '5.5xl': '3.5rem',
                        '6.5xl': '4rem'
                    },
                    spacing: {
                        '18': '4.5rem',
                        '68': '17rem',
                        '100': '25rem',
                        '120': '30rem'
                    },
                    height: {
                        '100': '25rem',
                        '120': '30rem',
                        '144': '36rem'
                    },
                    width: {
                        '100': '25rem',
                        '120': '30rem',
                        '144': '36rem'
                    },
                    boxShadow: {
                        'soft': '0 5px 20px rgba(0, 0, 0, 0.05)',
                        'medium': '0 8px 30px rgba(0, 0, 0, 0.08)',
                        'large': '0 15px 50px rgba(0, 0, 0, 0.1)',
                        'inner-bottom': 'inset 0 -2px 10px rgba(0, 0, 0, 0.06)',
                        'inner-top': 'inset 0 2px 10px rgba(0, 0, 0, 0.06)',
                        'highlight': '0 10px 40px rgba(184, 134, 11, 0.15)'
                    }
                }
            }
        }
    </script>
    
    <!-- Additional CSS -->
    <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
    
    <style>
        html {
            scroll-behavior: smooth;
        }
        
        body {
            font-family: 'Urbanist', sans-serif;
            color: #252525;
            overflow-x: hidden;
            background-color: #F9F6F0;
        }
        
        h1, h2, h3, h4, h5, h6 {
            font-family: 'Cormorant Garamond', serif;
            font-weight: 500;
        }
        
        /* Decorative elements */
        .decorative-line {
            position: relative;
        }
        
        .decorative-line::after {
            content: '';
            position: absolute;
            bottom: -15px;
            left: 0;
            width: 60px;
            height: 1px;
            background-color: #B8860B;
        }
        
        .decorative-line.centered::after {
            left: 50%;
            transform: translateX(-50%);
        }
        
        /* Transfer card styles */
        .transfer-card {
            position: relative;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
            background-color: white;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
        }
        
        .transfer-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        }
        
        .transfer-card .img-container {
            overflow: hidden;
        }
        
        .transfer-card img {
            transition: all 1.2s cubic-bezier(0.165, 0.84, 0.44, 1);
        }
        
        .transfer-card:hover img {
            transform: scale(1.1);
        }
        
        /* Button styles */
        .btn-primary {
            display: inline-block;
            background-color: #B8860B;
            color: white;
            font-size: 15px;
            font-weight: 500;
            letter-spacing: 0.5px;
            padding: 12px 30px;
            border: 1px solid #B8860B;
            position: relative;
            overflow: hidden;
            z-index: 1;
            transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
        }
        
        .btn-primary::after {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 0;
            height: 100%;
            background-color: white;
            z-index: -1;
            transition: width 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
        }
        
        .btn-primary:hover {
            color: #B8860B;
        }
        
        .btn-primary:hover::after {
            width: 100%;
        }
        
        /* Filter button styles */
        .filter-btn {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            font-size: 0.875rem;
            font-weight: 500;
            background-color: #F5F1E9;
            color: #252525;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .filter-btn:hover, .filter-btn.active {
            background-color: #B8860B;
            color: white;
        }
        
        /* Section label style */
        .section-label {
            writing-mode: vertical-rl;
            text-orientation: mixed;
            text-transform: uppercase;
            letter-spacing: 4px;
            font-size: 12px;
            color: #B8860B;
            position: absolute;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
        }
        
        /* Hover link animation */
        .hover-link {
            position: relative;
            text-decoration: none;
            transition: color 0.3s;
        }
        
        .hover-link::after {
            content: '';
            position: absolute;
            width: 100%;
            height: 1px;
            bottom: -2px;
            left: 0;
            background-color: #B8860B;
            transform: scaleX(0);
            transform-origin: right;
            transition: transform 0.3s ease-out;
        }
        
        .hover-link:hover::after {
            transform: scaleX(1);
            transform-origin: left;
        }
    </style>
</head>
<body class="bg-cream">
    <%- include('../partials/navbar') %>
    
    <!-- Hero Section -->
    <section class="relative py-20 md:py-28 bg-charcoal overflow-hidden">
        <!-- Background Image with Overlay -->
        <div class="absolute inset-0 z-0">
            <img src="/images/morocco-transfer.jpg" alt="Morocco Transfers" class="w-full h-full object-cover opacity-40">
            <div class="absolute inset-0 bg-gradient-to-r from-charcoal via-charcoal/80 to-charcoal/70"></div>
        </div>
        
        <!-- Hero Content -->
        <div class="container mx-auto px-6 md:px-12 relative z-10">
            <div class="max-w-3xl" data-aos="fade-up">
                <span class="inline-block mb-4 text-primary uppercase tracking-widest text-sm font-medium">Travel in Comfort</span>
                <h1 class="text-4xl md:text-5xl lg:text-6xl font-serif font-medium mb-6 text-white leading-tight">
                    City <span class="text-primary">Transfers</span>
                </h1>
                <p class="text-lg md:text-xl text-white/80 mb-8 max-w-2xl">
                    Travel between Morocco's enchanting cities in comfort and style with our reliable private transfer services. Professional drivers, comfortable vehicles, and stress-free journeys.
                </p>
                <a href="/transfers/custom" class="btn-primary">
                    <i class="fas fa-car-side mr-2"></i> Request Custom Transfer
                </a>
            </div>
        </div>
    </section>
    
    <!-- Transfers Section -->
    <section class="py-16 relative">
        <div class="section-label">Transfers</div>
        
        <div class="container mx-auto px-6 md:px-12">
            <div class="mb-12 max-w-3xl" data-aos="fade-up">
                <span class="inline-block mb-3 text-primary uppercase tracking-widest text-sm font-medium">Seamless Transportation</span>
                <h2 class="text-2xl md:text-3xl font-serif font-medium mb-0 leading-tight decorative-line">
                    Popular Transfer Routes
                </h2>
            </div>
            
            <!-- Filter Buttons -->
            <div class="flex flex-wrap gap-3 mt-8 mb-10" data-aos="fade-up">
                <button class="filter-btn active" data-filter="all">All Transfers</button>
                <button class="filter-btn" data-filter="marrakech">From Marrakech</button>
                <button class="filter-btn" data-filter="fez">From Fez</button>
                <button class="filter-btn" data-filter="casablanca">From Casablanca</button>
                <button class="filter-btn" data-filter="tangier">From Tangier</button>
            </div>
            
            <!-- Transfers Grid -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8" data-aos="fade-up" data-aos-delay="100">
                <% if (transfers && transfers.length > 0) { %>
                    <% transfers.forEach((transfer, index) => { %>
                        <div class="transfer-card h-full" 
                             data-aos="fade-up" 
                             data-aos-delay="<%= index * 50 %>"
                             data-start="<%= transfer.startCity.toLowerCase() %>" 
                             data-end="<%= transfer.endCity.toLowerCase() %>">
                            <div class="img-container h-56 relative">
                                <% if (transfer.gallery && transfer.gallery.length > 0) { %>
                                    <img src="<%= transfer.gallery[0] %>" alt="<%= transfer.startCity %> to <%= transfer.endCity %>" class="w-full h-full object-cover">
                                <% } else { %>
                                    <img src="/images/placeholder-transfer.jpg" alt="<%= transfer.startCity %> to <%= transfer.endCity %>" class="w-full h-full object-cover">
                                <% } %>
                                <div class="absolute top-4 right-4 bg-primary text-white py-1 px-3 text-xs uppercase tracking-wider">
                                    <%= transfer.vehicleType %>
                                </div>
                            </div>
                            
                            <div class="p-6 flex flex-col h-full">
                                <div>
                                    <a href="/transfers/<%= transfer.slug %>" class="block mb-4">
                                        <h3 class="text-xl font-serif font-medium hover:text-primary transition-colors leading-tight">
                                            <%= transfer.startCity %> to <%= transfer.endCity %>
                                        </h3>
                                    </a>
                                    
                                    <div class="flex justify-between items-center mb-6">
                                        <div class="flex items-center text-sm text-secondary/70">
                                            <i class="far fa-clock text-primary mr-2"></i>
                                            <span><%= transfer.duration %></span>
                                        </div>
                                        
                                        <div class="text-xl font-semibold text-primary">
                                            <%= transfer.price %> EUR
                                        </div>
                                    </div>
                                    
                                    <div class="space-y-2 mb-6">
                                        <div class="flex items-center text-sm text-secondary/70">
                                            <i class="fas fa-users text-primary w-5 mr-2"></i>
                                            <span>Up to <%= transfer.maxPassengers %> passengers</span>
                                        </div>
                                        <div class="flex items-center text-sm text-secondary/70">
                                            <i class="fas fa-car text-primary w-5 mr-2"></i>
                                            <span><%= transfer.vehicleType %></span>
                                        </div>
                                        <div class="flex items-center text-sm text-secondary/70">
                                            <i class="fas fa-calendar-alt text-primary w-5 mr-2"></i>
                                            <span>Available daily</span>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="mt-auto">
                                    <a href="/transfers/<%= transfer.slug %>" class="btn-primary w-full text-center">
                                        View Details
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="col-span-3 text-center py-16" data-aos="fade-up">
                        <div class="w-16 h-16 mx-auto rounded-full border-2 border-primary/20 flex items-center justify-center mb-4">
                            <i class="fas fa-car-side text-primary/40 text-xl"></i>
                        </div>
                        <h3 class="text-xl font-serif text-secondary/70 mb-2">No Transfers Available</h3>
                        <p class="text-secondary/60 text-sm mb-8">Please check back later or request a custom transfer.</p>
                        <a href="/transfers/custom" class="btn-primary">
                            Request Custom Transfer
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </section>
    
    <!-- Features Section -->
    <section class="py-16 bg-white">
        <div class="container mx-auto px-6 md:px-12">
            <div class="text-center mb-12" data-aos="fade-up">
                <span class="inline-block mb-3 text-primary uppercase tracking-widest text-sm font-medium">Why Choose Our Transfers</span>
                <h2 class="text-2xl md:text-3xl font-serif font-medium mb-0 leading-tight decorative-line centered">
                    Transfer Service Benefits
                </h2>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-16">
                <div class="text-center" data-aos="fade-up" data-aos-delay="0">
                    <div class="w-16 h-16 mx-auto rounded-full bg-beige flex items-center justify-center mb-4">
                        <i class="fas fa-shield-alt text-primary text-xl"></i>
                    </div>
                    <h3 class="text-xl font-serif font-medium mb-3">Safety & Reliability</h3>
                    <p class="text-secondary/70">
                        Professional drivers with excellent safety records and well-maintained vehicles ensure a secure and reliable journey.
                    </p>
                </div>
                
                <div class="text-center" data-aos="fade-up" data-aos-delay="100">
                    <div class="w-16 h-16 mx-auto rounded-full bg-beige flex items-center justify-center mb-4">
                        <i class="fas fa-couch text-primary text-xl"></i>
                    </div>
                    <h3 class="text-xl font-serif font-medium mb-3">Comfort & Convenience</h3>
                    <p class="text-secondary/70">
                        Modern air-conditioned vehicles with spacious interiors, door-to-door service, and flexible scheduling for your convenience.
                    </p>
                </div>
                
                <div class="text-center" data-aos="fade-up" data-aos-delay="200">
                    <div class="w-16 h-16 mx-auto rounded-full bg-beige flex items-center justify-center mb-4">
                        <i class="fas fa-gem text-primary text-xl"></i>
                    </div>
                    <h3 class="text-xl font-serif font-medium mb-3">Premium Experience</h3>
                    <p class="text-secondary/70">
                        Enjoy a luxurious travel experience with English-speaking drivers, complimentary water, and Wi-Fi in selected vehicles.
                    </p>
                </div>
            </div>
        </div>
    </section>
    
    <!-- Custom Transfer CTA -->
    <section class="py-16 bg-beige">
        <div class="container mx-auto px-6 md:px-12">
            <div class="max-w-4xl mx-auto bg-white p-8 md:p-12 shadow-soft" data-aos="fade-up">
                <div class="text-center">
                    <span class="inline-block mb-3 text-primary uppercase tracking-widest text-sm font-medium">Personalized Service</span>
                    <h2 class="text-2xl md:text-3xl font-serif font-medium mb-5 leading-tight decorative-line centered">
                        Need a Custom Transfer?
                    </h2>
                    <p class="text-secondary/70 mb-8 max-w-2xl mx-auto">
                        We can arrange transfers between any cities in Morocco tailored to your specific needs and schedule. Our custom transfer service offers flexibility and convenience for your unique travel requirements.
                    </p>
                    
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-left mb-10">
                        <div class="bg-beige p-4">
                            <div class="flex items-center mb-3">
                                <div class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center mr-3">
                                    <i class="fas fa-route text-primary"></i>
                                </div>
                                <h3 class="text-lg font-serif">Any Route</h3>
                            </div>
                            <p class="text-sm text-secondary/70">
                                Travel between any cities or locations in Morocco, no matter how remote.
                            </p>
                        </div>
                        
                        <div class="bg-beige p-4">
                            <div class="flex items-center mb-3">
                                <div class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center mr-3">
                                    <i class="fas fa-users text-primary"></i>
                                </div>
                                <h3 class="text-lg font-serif">Any Group Size</h3>
                            </div>
                            <p class="text-sm text-secondary/70">
                                From solo travelers to large groups, we have vehicles to accommodate all party sizes.
                            </p>
                        </div>
                        
                        <div class="bg-beige p-4">
                            <div class="flex items-center mb-3">
                                <div class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center mr-3">
                                    <i class="fas fa-calendar-alt text-primary"></i>
                                </div>
                                <h3 class="text-lg font-serif">Flexible Scheduling</h3>
                            </div>
                            <p class="text-sm text-secondary/70">
                                Choose your own departure times and add stops along the way to customize your journey.
                            </p>
                        </div>
                    </div>
                    
                    <a href="/transfers/custom" class="btn-primary">
                        <i class="fas fa-car-side mr-2"></i> Request Custom Transfer
                    </a>
                </div>
            </div>
        </div>
    </section>
    
    <%- include('../partials/footer') %>
    
    <!-- AOS Animation Library -->
    <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize AOS animations
            AOS.init({
                duration: 800,
                once: true,
                offset: 100,
                easing: 'ease-out-cubic'
            });
            
            // Filter functionality
            const filterButtons = document.querySelectorAll('.filter-btn');
            const transferCards = document.querySelectorAll('.transfer-card');
            
            filterButtons.forEach(button => {
                button.addEventListener('click', function() {
                    // Remove active class from all buttons
                    filterButtons.forEach(btn => btn.classList.remove('active'));
                    
                    // Add active class to clicked button
                    this.classList.add('active');
                    
                    const filter = this.dataset.filter;
                    
                    // Show/hide transfers based on filter
                    transferCards.forEach(card => {
                        if (filter === 'all') {
                            card.style.display = 'block';
                        } else {
                            const startCity = card.dataset.start;
                            if (startCity.includes(filter)) {
                                card.style.display = 'block';
                            } else {
                                card.style.display = 'none';
                            }
                        }
                    });
                });
            });
        });
    </script>
</body>
</html> 